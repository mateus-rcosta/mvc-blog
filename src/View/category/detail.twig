{% extends "layout.twig" %}

{% block content %}
    {% if category %}
        <h1>Categoria: {{ category.nome }}</h1>
        {% if category.descricao %}
            <p>{{ category.descricao }}</p>
        {% endif %}
        <p><strong>ID:</strong> {{ category.id }}</p>
    {% else %}
        <p>Categoria não encontrada.</p>
    {% endif %}
    <a href="/category" class="btn btn-secondary">Voltar para lista</a>
{% endblock %}
